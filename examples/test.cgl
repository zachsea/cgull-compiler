struct Weird {
  float z;
  int* p;

  fn test(int z) -> float {
    return (z as float) + this.z;
  }
}

struct Point {
  int x;
  int y;
  Weird w;
}

fn main() {
  int* ptr = allocate int(10);
  Point p = Point(1, 2, Weird(3.0, ptr));
  println(p.w.test(10));
  return;
}
